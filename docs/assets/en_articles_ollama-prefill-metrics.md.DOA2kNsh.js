import{_ as t,o as a,c as o,ai as r}from"./chunks/framework.C33Uq6m-.js";const p=JSON.parse('{"title":"Ollama 0.33.3 changed what prompt_eval_duration measures","description":"","frontmatter":{},"headers":[],"relativePath":"en/articles/ollama-prefill-metrics.md","filePath":"en/articles/ollama-prefill-metrics.md"}'),n={name:"en/articles/ollama-prefill-metrics.md"};function s(d,e,i,h,c,l){return a(),o("div",null,[...e[0]||(e[0]=[r('<h1 id="ollama-0-33-3-changed-what-prompt-eval-duration-measures" tabindex="-1">Ollama 0.33.3 changed what <code>prompt_eval_duration</code> measures <a class="header-anchor" href="#ollama-0-33-3-changed-what-prompt-eval-duration-measures" aria-label="Permalink to &quot;Ollama 0.33.3 changed what `prompt_eval_duration` measures&quot;">​</a></h1><p>If you compute prefill throughput from Ollama&#39;s metrics the way everyone has always computed it, your numbers have been wrong since 0.33.3 — and wrong in the flattering direction.</p><p>On a warm cache, my own tooling reported <strong>4375 tok/s</strong> where the honest figure was <strong>115 tok/s</strong>. That&#39;s a 38× over-report, with no error, no warning, and no version signal beyond the presence of a new field.</p><h2 id="what-changed" tabindex="-1">What changed <a class="header-anchor" href="#what-changed" aria-label="Permalink to &quot;What changed&quot;">​</a></h2><p>0.33.3 added <code>prompt_eval_cached_count</code> to the responses from <code>/api/generate</code> and <code>/api/chat</code>, and <strong>redefined <code>prompt_eval_duration</code> to cover only the <em>uncached</em> prompt tokens.</strong></p><p><code>prompt_eval_count</code> did not change. It still means the total prompt size, cached tokens included.</p><p>So the standard formula —</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>prompt_eval_count / prompt_eval_duration</span></span></code></pre></div><p>— now divides the <em>total</em> token count by the time taken to evaluate only <em>some</em> of them. The error scales with your cache hit ratio, which means it&#39;s smallest when you&#39;re testing cold and largest in exactly the steady-state conditions you actually care about.</p><p>Here are two consecutive identical requests against qwen2.5:7b:</p><table tabindex="0"><thead><tr><th></th><th><code>prompt_eval_count</code></th><th><code>prompt_eval_cached_count</code></th><th><code>prompt_eval_duration</code></th><th>Old formula</th><th>Actual</th></tr></thead><tbody><tr><td>Cold</td><td>38</td><td>0</td><td>14495 ms</td><td>2.6 tok/s</td><td>2.6 tok/s</td></tr><tr><td>Warm</td><td>38</td><td>37</td><td>8.7 ms</td><td><strong>4375 tok/s</strong></td><td><strong>115 tok/s</strong></td></tr></tbody></table><p>The cold row is fine, which is part of why this is easy to miss — a quick sanity check on a fresh daemon looks perfectly reasonable.</p><h2 id="the-fix" tabindex="-1">The fix <a class="header-anchor" href="#the-fix" aria-label="Permalink to &quot;The fix&quot;">​</a></h2><p>Divide by the tokens that were actually evaluated:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>prompt_eval_count - prompt_eval_cached_count</span></span></code></pre></div><p>Ollama&#39;s own <code>Metrics.Summary()</code> was changed to do exactly this. The field is <code>omitempty</code>, so it&#39;s absent on older daemons and on runners that don&#39;t report it — worth treating &quot;absent&quot; as <em>unknown</em> rather than as zero, since those are different claims and only one of them is a problem.</p><h2 id="the-same-fact-under-three-different-names" tabindex="-1">The same fact under three different names <a class="header-anchor" href="#the-same-fact-under-three-different-names" aria-label="Permalink to &quot;The same fact under three different names&quot;">​</a></h2><p>Ollama exposes the same information through three protocols, with three different shapes:</p><table tabindex="0"><thead><tr><th>Endpoint</th><th>Field</th></tr></thead><tbody><tr><td><code>/api/chat</code> (native)</td><td><code>prompt_eval_cached_count</code></td></tr><tr><td><code>/v1/chat/completions</code> (OpenAI-compatible)</td><td><code>usage.prompt_tokens_details.cached_tokens</code></td></tr><tr><td><code>/v1/messages</code> (Anthropic-compatible)</td><td><code>usage.cache_read_input_tokens</code></td></tr></tbody></table><p>The Anthropic-compatible endpoint has a second trap. <code>input_tokens</code> there quietly stopped meaning the prompt total and started meaning <strong>total minus cached</strong>. Verified live on 0.33.3: an identical prompt reported <code>input_tokens: 38</code> cold and <code>input_tokens: 1</code> warm. If you&#39;re summing that field for accounting, your totals dropped and nothing told you.</p><h2 id="why-the-numbers-move-so-much" tabindex="-1">Why the numbers move so much <a class="header-anchor" href="#why-the-numbers-move-so-much" aria-label="Permalink to &quot;Why the numbers move so much&quot;">​</a></h2><p>The reason a cache hit changes prefill by two orders of magnitude, rather than a few percent, is that KV cache reuse is all-or-nothing from a given point: the cache holds only for as long as the prompt matches from the very first token, and the moment one token differs, everything after it is discarded and re-evaluated.</p><p>The practical consequence — put stable content first and volatile content last — is <a href="https://developers.openai.com/api/docs/guides/prompt-caching" target="_blank" rel="noreferrer">documented by OpenAI</a> and by Anthropic and Bedrock, and it is not a discovery. What&#39;s less obvious is how brutal the cliff is locally, where nothing surfaces cache reuse at all. Same model, same 324-token system prompt, one 21-token timestamp moved from the top to the bottom:</p><table tabindex="0"><thead><tr><th>Timestamp position</th><th>Prompt tokens</th><th>Reused</th><th>Prefill</th></tr></thead><tbody><tr><td>At the start</td><td>324</td><td>4</td><td>64.6 ms</td></tr><tr><td>At the end</td><td>324</td><td>290</td><td>18.6 ms</td></tr></tbody></table><p>Identical words, identical token count, 3.5× the prefill time. The hosted providers document the advice; locally you get no feedback whatsoever about whether you&#39;re following it, which is how a prompt template can quietly cost you the entire system prompt on every turn for months.</p><h2 id="the-measurement-i-got-wrong-first" tabindex="-1">The measurement I got wrong first <a class="header-anchor" href="#the-measurement-i-got-wrong-first" aria-label="Permalink to &quot;The measurement I got wrong first&quot;">​</a></h2><p>Worth recording, because it looked like a result.</p><p>My first approach compared <em>how much of layout A&#39;s cache does layout B reuse</em> — original prompt versus rewritten prompt. The rewritten one scored terribly, and I nearly published that it made things worse. It differs from A near the front, so of course it reuses almost nothing of A&#39;s cache.</p><p>That&#39;s the wrong question. Real traffic never sends A and then B; it sends <strong>the same layout every turn with a fresh value</strong>. The thing to measure is whether a layout survives <em>its own</em> next turn — so each layout has to be sent twice, once with its own values and once with them changed, and it&#39;s the second send that counts. Measuring the first only tells you how well a prompt matches itself, which is ~100% for every possible layout.</p><p>I caught it only because the numbers came back inverted against a real daemon. No unit test I would have written would have found it; the code did exactly what I told it to.</p><h2 id="limits" tabindex="-1">Limits <a class="header-anchor" href="#limits" aria-label="Permalink to &quot;Limits&quot;">​</a></h2><p>One model, one daemon, one prompt size, llama.cpp runner. I haven&#39;t tested across model families, at long context, under concurrency, or on the MLX runner. The metrics change is general and verifiable from the source; the specific 3.5× ratio is an illustration, not a benchmark, and I wouldn&#39;t quote it as one.</p><hr><p><em>I maintain <a href="https://github.com/LogneBudo/llmxray" target="_blank" rel="noreferrer">LLMxRay</a>, a local observability interface for Ollama. The regression above was found in its own code, and it now has a page that measures cache reuse against your daemon rather than estimating it. Apache-2.0, localhost only, <code>npx llmxray</code>. None of the above needs it — two curl calls and <code>prompt_eval_cached_count</code> reproduce every table here.</em></p>',34)])])}const u=t(n,[["render",s]]);export{p as __pageData,u as default};
