import{_ as t,o as a,c as o,ai as s}from"./chunks/framework.C33Uq6m-.js";const c=JSON.parse('{"title":"Cache Lab","description":"","frontmatter":{},"headers":[],"relativePath":"en/guide/cache-lab.md","filePath":"en/guide/cache-lab.md"}'),r={name:"en/guide/cache-lab.md"};function n(i,e,h,l,d,u){return a(),o("div",null,[...e[0]||(e[0]=[s('<h1 id="cache-lab" tabindex="-1">Cache Lab <a class="header-anchor" href="#cache-lab" aria-label="Permalink to &quot;Cache Lab&quot;">​</a></h1><p>The <strong>Cache Lab</strong> tells you why your prompt misses the model&#39;s cache, and measures what that costs you on every single turn.</p><p><strong>Sidebar item:</strong> Cache Lab (above Protocol Observatory) <strong>Route:</strong> <code>/cache-lab</code><strong>Added in:</strong> v0.6.0 (September 2026) <strong>Requires:</strong> Ollama 0.33.3 or newer</p><h2 id="what-s-the-point" tabindex="-1">What&#39;s the point? <a class="header-anchor" href="#what-s-the-point" aria-label="Permalink to &quot;What&#39;s the point?&quot;">​</a></h2><p>A local model doesn&#39;t re-read your whole prompt every turn. It keeps a <strong>KV cache</strong> of the tokens it has already processed, and reuses it — but only for as long as the prompt <strong>still matches from the very first token</strong>.</p><p>That &quot;from the very first token&quot; is the whole story. The cache survives on a shared <em>prefix</em>. The moment one token differs, everything after it is thrown away and has to be evaluated again, no matter how much of it was identical.</p><p>So a single timestamp at the top of a system prompt costs you the entire system prompt, every turn. Measured on a real daemon with the same 324-token prompt:</p><table tabindex="0"><thead><tr><th>Where the timestamp sits</th><th>Prompt tokens</th><th>Reused from cache</th><th>Prefill</th></tr></thead><tbody><tr><td>At the <strong>start</strong></td><td>324</td><td>4</td><td>64.6 ms</td></tr><tr><td>At the <strong>end</strong></td><td>324</td><td>290</td><td>18.6 ms</td></tr></tbody></table><p>Same model. Same words. Same token count. <strong>3.5× the prefill time</strong>, purely because of where one value sat.</p><p>This is invisible in every other tool, and it is one of the largest levers on how fast a local model feels.</p><h2 id="using-the-page" tabindex="-1">Using the page <a class="header-anchor" href="#using-the-page" aria-label="Permalink to &quot;Using the page&quot;">​</a></h2><ol><li>Pick a model.</li><li>Paste the system prompt you actually send every turn.</li><li>Click <strong>Measure</strong>.</li></ol><p>The page does three things.</p><h3 id="diagnosis" tabindex="-1">Diagnosis <a class="header-anchor" href="#diagnosis" aria-label="Permalink to &quot;Diagnosis&quot;">​</a></h3><p>Before measuring anything, the lab scans your prompt for values that change between turns — timestamps, dates, times, session ids, long numbers — and shows them as chips.</p><p>It then renders your prompt in three colours:</p><ul><li><strong>Green</strong> — the prefix the cache can keep.</li><li><strong>Red</strong> — the first value that changes.</li><li><strong>Grey</strong> — everything forfeited, only because it sits <em>after</em> that value.</li></ul><p>The grey region is the point. It is usually most of the prompt, and there is usually nothing wrong with it.</p><h3 id="measurement" tabindex="-1">Measurement <a class="header-anchor" href="#measurement" aria-label="Permalink to &quot;Measurement&quot;">​</a></h3><p>The lab sends <strong>two layouts</strong>, each of them <strong>twice</strong>:</p><ol><li>Your prompt as written.</li><li>The same prompt with the changing values moved to the end.</li></ol><p>The second send of each layout carries a <em>different</em> value — a new timestamp, a new id — because that is what real traffic does. It is the second send that gets measured. Measuring the first would only tell you how well a prompt matches itself, which is ~100% for any layout and tells you nothing.</p><p>You get a table of what the daemon actually kept: prompt tokens, reused, evaluated, prefill time, and reuse percentage.</p><h3 id="verdict" tabindex="-1">Verdict <a class="header-anchor" href="#verdict" aria-label="Permalink to &quot;Verdict&quot;">​</a></h3><p>Finally, a plain statement of what the rewrite bought: tokens saved per turn, milliseconds saved per turn, and the speed-up.</p><p>Every number on the page came back from your own daemon. <strong>Nothing here is estimated.</strong></p><h2 id="reading-the-results" tabindex="-1">Reading the results <a class="header-anchor" href="#reading-the-results" aria-label="Permalink to &quot;Reading the results&quot;">​</a></h2><p><strong>Reuse near 100%</strong> — this layout is doing well. The cache is carrying nearly the whole prompt.</p><p><strong>Reuse near 0% with a value at the front</strong> — the usual case, and the one worth fixing. Move the changing values to the end of the system prompt, or into the user message, and the body above them stays cached.</p><p><strong>&quot;not reported&quot;</strong> — your daemon is older than Ollama 0.33.3 and doesn&#39;t report cache reuse. The lab will not show this as 0%, because &quot;the daemon didn&#39;t say&quot; and &quot;nothing was cached&quot; are different claims and only one of them is a problem.</p><p><strong>A negative saving</strong> — the rewrite made things worse. It happens; it is measured, so believe it rather than the theory.</p><h2 id="what-to-do-about-it" tabindex="-1">What to do about it <a class="header-anchor" href="#what-to-do-about-it" aria-label="Permalink to &quot;What to do about it&quot;">​</a></h2><p>The fix is nearly always structural, and it is nearly always cheap:</p><ul><li>Put the <strong>stable</strong> part of your system prompt first: role, rules, style, examples.</li><li>Put anything that <strong>changes per turn</strong> last: the current time, the user id, the session id, the retrieved chunks.</li><li>Keep <strong>retrieved context in a stable order</strong>. Re-ranking chunks between turns invalidates the cache from the first chunk that moved.</li><li>Don&#39;t rebuild the system prompt from a template that re-serialises a dictionary — key order can change under you.</li></ul><p>None of this changes what the model is told. It only changes where it is told, which is free.</p><h2 id="notes-and-limits" tabindex="-1">Notes and limits <a class="header-anchor" href="#notes-and-limits" aria-label="Permalink to &quot;Notes and limits&quot;">​</a></h2><ul><li>The lab holds generation to a single token. It measures <strong>prefill</strong>, and decoding would be pure noise here.</li><li>The rewrite it measures is mechanical: it moves the detected spans to the end and leaves everything else alone. It is a measurement, not a suggested final prompt — read it before adopting it.</li><li>The mutated &quot;next turn&quot; value is shape-preserving, not meaningful. A date may come out impossible. That is fine: the cache only cares that the value changed, not what it means.</li><li>Everything runs against <code>localhost</code>. No cloud, no API key, no signup.</li></ul>',37)])])}const m=t(r,[["render",n]]);export{c as __pageData,m as default};
